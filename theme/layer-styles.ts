import { SystemStyleObject } from "@chakra-ui/styled-system";

export const home: Record<string, SystemStyleObject> = {
  container: {
    py: 24,
    px: 12,
  },
};

export const layerStyles = { home };
